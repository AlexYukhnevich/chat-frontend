// import { FETCH_USER } from './auth.constants';

const initialState = {
  user: null,
  hasError: false,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
